nelmio_api_doc:
    documentation:
        info:
            title: Publisher Api
            description: Api for publisher books and more
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas: # to filter documented areas
        path_patterns:
            - ^/api/v1
